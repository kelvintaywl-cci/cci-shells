version: 2.1

jobs:
  bash:
    docker:
      - image: cimg/base:stable
    steps:
      - run:
          name: fail on purpose
          command: |
            exit 1
          background: true
      - run:
          name: halt
          shell: '/bin/bash'
          command: |
            circleci-agent step halt
          when: on_fail  

workflows:
  taco-shells:
    jobs:
      - bash
